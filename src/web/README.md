# RepTracker web

React + Vite front end. See the [root README](../../README.md) for the full picture.

```bash
npm install
npm run dev     # :5173, proxies /api to the API on :5199
npm run build   # writes the built app into ../RepTracker.Api/wwwroot
```
